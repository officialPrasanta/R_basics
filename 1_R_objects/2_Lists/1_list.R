points = c(2, 3, 1)
axis = 'x-axis'
result_list = list(points, axis, sin)

print(result_list)

# two vectors inside a list

first_vec = c(2, 3, 6)
second_vec = c(4, 5, 20)

a_list = list(first_vec, second_vec)
print(a_list)

# class of a list
print(class(a_list))


